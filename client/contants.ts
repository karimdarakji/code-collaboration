export enum SessionVisibility {
  PUBLIC = "public",
  PRIVATE = "private",
}

export enum ButtonVariant {
  PRIMARY = "primary",
  SECONDARY = "secondary",
}

export enum AlertVariant {
  DANGER = "danger",
  INFO = "info",
}
